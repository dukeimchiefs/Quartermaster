sqlalchemy>=2.0
pytest>=8.0

# Audited 2026-07-04 (see CLAUDE.md's dependency guardrail): CP-SAT
# (ortools.sat.python.cp_model) is a fully local native solver — no network
# calls. The only network-capable code in the package is the opt-in
# ortools.math_opt.python.ipc.remote_http_solve helper, which POSTs to
# https://optimization.googleapis.com and requires an explicit API key to
# invoke. It is unrelated to CP-SAT and this codebase must never import or
# call it.
ortools>=9.0

# Audited 2026-07-06: Streamlit defaults to phoning home anonymous usage
# stats (browser.gatherUsageStats=True by default, POSTs are gated by that
# exact config flag in streamlit/runtime/metrics_util.py) and, on first run
# with no local credentials file, interactively prompts for an email address
# that — only if submitted — gets POSTed for a mailing list. Both are
# disabled in .streamlit/config.toml (gatherUsageStats=false, headless=true,
# which together fully skip both code paths per
# streamlit/runtime/credentials.py's check_credentials()). Do not remove
# that config file.
streamlit>=1.59

# Audited 2026-07-13: the `ollama` PyPI package (checked v0.6.2) is a thin
# httpx wrapper — every request goes to the single `host` passed to
# Client()/AsyncClient(), defaulting to http://127.0.0.1:11434
# (ollama/_client.py:_parse_host). No other network code in the package.
#
# The Ollama *server* itself (separate Homebrew install, not this package)
# ships a cloud feature (remote inference + web search against ollama.com,
# env vars OLLAMA_REMOTES/OLLAMA_NO_CLOUD) as of v0.31.1 — this is an
# outbound path that violates CLAUDE.md's no-cloud-inference constraint
# unless explicitly disabled. The server MUST be started with
# OLLAMA_NO_CLOUD=1 — always via scripts/run_ollama.sh, never `ollama serve`
# directly (a client library can't verify its own server's env, so this
# isn't something llm/client.py can check or enforce). Models pulled must
# be plain local tags (e.g. "llama3.1:8b"), never a "-cloud" suffixed tag,
# which routes to ollama.com regardless of OLLAMA_NO_CLOUD — this part
# llm/client.py does check and reject.
ollama>=0.6.0

# pandas ships as a transitive dependency of streamlit already, but
# app/pages/1_Build_Schedule.py imports it directly (to render the built
# schedule as a pivot table), so it's declared explicitly here too. Pure
# data-manipulation library, no network code.
pandas>=2.0

# Audited 2026-07 (see CLAUDE.md's dependency guardrail): openpyxl (checked
# v3.1.5) has zero network-related code — grepping the installed package
# tree, and its one dependency et_xmlfile, for urllib/requests/socket/
# http.client/httpx imports returns no matches. Pure local .xlsx/.xlsm
# reader/writer, no telemetry, no license-check phone-home.
#
# real_schedule/ uses it to read (never write) the real, live schedule
# workbooks at Resident_Schedules/ — see CLAUDE.md's PII boundary section.
# Every real_schedule/ reader calls load_workbook(path, read_only=True,
# data_only=True); read_only mode removes the write API from the returned
# object entirely (no .save() exists on it) — this is the actual
# enforcement mechanism for "never write to Resident_Schedules/", verified
# by tests/test_real_schedule_never_writes.py, not just a comment.
openpyxl>=3.1

# Audited 2026-07-16 (see CLAUDE.md's dependency guardrail): nicknames
# (checked v1.0.1) has zero network-related code and zero dependencies of
# its own — grepping the installed package tree for urllib/requests/
# socket/http.client/httpx imports returns no matches. It's a pure lookup
# over a bundled names.csv file (read via importlib.resources), used by
# llm/tools.py's free-text name resolver to close the gap prefix-matching
# alone can't ("Jack" -> "John", "Bill" -> "William" aren't literal
# prefixes of the formal name, unlike "Chris" -> "Christopher").
nicknames>=1.0
