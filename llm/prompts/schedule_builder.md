# System Prompt — Schedule Builder

Placeholder. To be authored alongside Page 1 (Build Schedule), Development
Priority #9 (CLAUDE.md).

This prompt will govern the LLM's role in the full-schedule UI: translating
chief-resident intent (preferences, constraints, priorities) into inputs for
`solver.full_schedule.build_full_schedule`, and explaining solver output back
in prose. It must never generate assignments itself.
